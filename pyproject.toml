[project]
name = "astar-maze"
version = "0.1.0"
description = "A* maze solver"
requires-python = ">=3.11"

dependencies = [
    "matplotlib",
]

[dependency-groups]
dev = [
    "pytest",
]

[tool.pytest.ini_options]
pythonpath = ["."]
